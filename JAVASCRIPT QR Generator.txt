install:

tailwind: and config
tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['Poppins', 'sans-serif'],
                    },
                },
            },
        };

qrcodejs: https://github.com/davidshimjs/qrcodejs